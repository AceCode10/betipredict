# Local development
DATABASE_URL="file:./dev.db"
NEXTAUTH_URL="http://localhost:3000"

# Production (PostgreSQL - Supabase)
# DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
# DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-a-secret-with: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""

# Football Data API (for live sports data & auto-resolution)
FOOTBALL_DATA_API_KEY=your_football_data_api_key_here

# Real-time Football API (for immediate match resolution)
# Sign up at https://www.api-football.com/
API_FOOTBALL_KEY=your_api_football_key_here

# Cron Secret (for automatic market resolution endpoint)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=your_cron_secret_here

# Webhook Secret (for instant match status webhooks)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WEBHOOK_SECRET=your_webhook_secret_here

# Admin emails (comma-separated, for market resolution permissions)
ADMIN_EMAILS=admin@betipredict.com

# Airtel Money API (Zambia - Mobile Money payments)
# Sign up at https://developers.airtel.africa/signup
# Create an app at https://developers.airtel.africa/developer/register
AIRTEL_MONEY_CLIENT_ID=your_airtel_client_id_here
AIRTEL_MONEY_CLIENT_SECRET=your_airtel_client_secret_here
AIRTEL_MONEY_PIN=your_airtel_disbursement_pin_here
# Webhook secret for verifying Airtel Money callback signatures
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AIRTEL_MONEY_WEBHOOK_SECRET=your_webhook_secret_here
# Set to "production" for live payments, anything else for sandbox/staging
AIRTEL_MONEY_ENV=staging

# MTN Mobile Money API (Zambia - MoMo payments)
# Sign up at https://momodeveloper.mtn.com/signup
# Subscribe to Collection & Disbursement products
MTN_MOMO_COLLECTION_KEY=a818c725f83e4d4da6bf6de539b1a60c
MTN_MOMO_DISBURSEMENT_KEY=2b2f4f08d68247d2b3ee0a22861def4a
MTN_MOMO_COLLECTION_USER=your_collection_api_user_uuid_here
MTN_MOMO_COLLECTION_API_KEY=your_collection_api_key_here
MTN_MOMO_DISBURSEMENT_USER=your_disbursement_api_user_uuid_here
MTN_MOMO_DISBURSEMENT_API_KEY=your_disbursement_api_key_here
# Webhook secret for verifying MTN MoMo callback signatures
MTN_MOMO_WEBHOOK_SECRET=your_mtn_webhook_secret_here
# Set to "production" for live payments, anything else for sandbox
MTN_MOMO_ENV=sandbox

# Blockchain (optional)
SEPOLIA_URL=https://sepolia.infura.io/v3/YOUR_INFURA_PROJECT_ID
PRIVATE_KEY=your_metaMask_private_key_here
